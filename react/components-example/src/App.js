import Header from './components/Header.js'
function App() {
  return (
    <div>
      <Header />
    </div>
  );
}

export default App;

/*Top level parent component
Often called App

Each component must be imported, can only be done if it is exported from the file.

Syntax: 
  import Component from <file path>
  function
  export default Component
 */